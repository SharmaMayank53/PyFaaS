# SOPM
OPM is a self-hosted serverless platform for running Python functions from ZIP uploads. It includes a FastAPI backend, a Next.js dashboard, PostgreSQL, Redis, MinIO, workers, scheduling, API-key invocation, and Prometheus/Grafana monitoring.
