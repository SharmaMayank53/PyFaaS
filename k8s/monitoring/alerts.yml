groups:
  - name: sopm.platform
    interval: 30s
    rules:
      # -----------------------------------------------------------------------
      # Availability
      # -----------------------------------------------------------------------
      - alert: PyFaaSApiDown
        expr: up{job="sopm-api"} == 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "PyFaaS API is down"
          description: "The PyFaaS API has been unreachable for more than 1 minute."

      - alert: PyFaaSWorkerDown
        expr: sopm_worker_active == 0
        for: 2m
        labels:
          severity: critical
        annotations:
          summary: "No active PyFaaS workers"
          description: "No workers are active. Executions will not be processed."

      # -----------------------------------------------------------------------
      # Execution health
      # -----------------------------------------------------------------------
      - alert: PyFaaSHighExecutionFailureRate
        expr: |
          rate(sopm_executions_total{status="FAILED"}[5m])
          /
          rate(sopm_executions_total[5m]) > 0.1
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "High execution failure rate (>10%)"
          description: "{{ $value | humanizePercentage }} of executions are failing."

      - alert: PyFaaSExecutionTimeoutRate
        expr: |
          rate(sopm_executions_total{status="TIMED_OUT"}[10m])
          /
          rate(sopm_executions_total[10m]) > 0.05
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "High execution timeout rate (>5%)"

      # -----------------------------------------------------------------------
      # Queue health
      # -----------------------------------------------------------------------
      - alert: PyFaaSQueueDepthHigh
        expr: sopm_queue_depth > 500
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "PyFaaS queue depth is high"
          description: "Queue depth is {{ $value }}. Workers may be lagging."

      - alert: PyFaaSQueueDepthCritical
        expr: sopm_queue_depth > 2000
        for: 2m
        labels:
          severity: critical
        annotations:
          summary: "PyFaaS queue depth is critical ({{ $value }})"

      - alert: PyFaaSDLQGrowing
        expr: increase(sopm_dlq_depth[15m]) > 10
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "PyFaaS dead-letter queue is growing"
          description: "{{ $value }} jobs moved to DLQ in last 15 minutes."

      # -----------------------------------------------------------------------
      # Latency
      # -----------------------------------------------------------------------
      - alert: PyFaaSApiHighLatency
        expr: |
          histogram_quantile(0.95,
            rate(sopm_http_request_duration_seconds_bucket{endpoint!="/metrics"}[5m])
          ) > 2
        for: 5m
        labels:
          severity: warning
        annotations:
          summary: "PyFaaS API p95 latency > 2s"
          description: "p95 latency is {{ $value | humanizeDuration }}."

      # -----------------------------------------------------------------------
      # Infrastructure
      # -----------------------------------------------------------------------
      - alert: PyFaaSPostgresDown
        expr: pg_up == 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "PostgreSQL is down"

      - alert: PyFaaSRedisDown
        expr: redis_up == 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "Redis is down"
