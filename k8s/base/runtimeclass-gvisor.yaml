---
# =============================================================================
# gVisor RuntimeClass
# =============================================================================
# This requires runsc/gVisor to be installed on every node that can schedule
# SOPM sandbox pods. The handler name must match the container runtime config.
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: gvisor
handler: runsc